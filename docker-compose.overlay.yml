services:
  overlay-proxy:
    image: nginx:alpine
    restart: unless-stopped
    volumes:
      - ./overlay/nginx.conf:/etc/nginx/nginx.conf:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
